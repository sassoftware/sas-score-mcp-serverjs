---
resource: "scr"
description: "Decision tree for SCR models: list, find, describe, score"
intents:
  list:
    exec:
      tool: "sas-score-list-scr"
      args:
        start: 1
        limit: 10
    format: "list of SCR models"
  find:
    parse:
      name: "$name"
    exec:
      tool: "sas-score-find-scr"
      args:
        name: "$name"
    format: "find response"
  describe:
    parse:
      name: "$name"
    # SCR: skip find-* verification; describe directly
    exec:
      tool: "sas-score-scr-describe"
      args:
        name: "$name"
    format: "SCR model schema and metadata"
  score:
    parse:
      name: "$name"
      scenario: "$scenario"
    # SCR: skip find-* verification; score directly
    exec:
      tool: "sas-score-scr-score"
      args:
        name: "$name"
        scenario: "$scenario"
    format: "merge prediction with input"
